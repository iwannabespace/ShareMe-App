#include "../../include/utils/theme.hpp"

namespace ShareMe
{
    const sf::Color Theme::Primary = sf::Color(170, 199, 255);
    const sf::Color Theme::SurfaceTint = sf::Color(170, 199, 255);
    const sf::Color Theme::OnPrimary = sf::Color(10, 48, 95);
    const sf::Color Theme::PrimaryContainer = sf::Color(40, 71, 119);
    const sf::Color Theme::OnPrimaryContainer = sf::Color(214, 227, 255);
    const sf::Color Theme::Secondary = sf::Color(190, 198, 220);
    const sf::Color Theme::OnSecondary = sf::Color(40, 49, 65);
    const sf::Color Theme::SecondaryContainer = sf::Color(62, 71, 89);
    const sf::Color Theme::OnSecondaryContainer = sf::Color(218, 226, 249);
    const sf::Color Theme::Tertiary = sf::Color(221, 188, 224);
    const sf::Color Theme::OnTertiary = sf::Color(63, 40, 68);
    const sf::Color Theme::TertiaryContainer = sf::Color(87, 62, 92);
    const sf::Color Theme::OnTertiaryContainer = sf::Color(250, 216, 253);
    const sf::Color Theme::Error = sf::Color(255, 180, 171);
    const sf::Color Theme::OnError = sf::Color(105, 0, 5);
    const sf::Color Theme::ErrorContainer = sf::Color(147, 0, 10);
    const sf::Color Theme::OnErrorContainer = sf::Color(255, 218, 214);
    const sf::Color Theme::Background = sf::Color(17, 19, 24);
    const sf::Color Theme::OnBackground = sf::Color(226, 226, 233);
    const sf::Color Theme::Surface = sf::Color(17, 19, 24);
    const sf::Color Theme::OnSurface = sf::Color(226, 226, 233);
    const sf::Color Theme::SurfaceVariant = sf::Color(68, 71, 78);
    const sf::Color Theme::OnSurfaceVariant = sf::Color(196, 198, 208);
    const sf::Color Theme::Outline = sf::Color(142, 144, 153);
    const sf::Color Theme::OutlineVariant = sf::Color(68, 71, 78);
    const sf::Color Theme::Shadow = sf::Color(0, 0, 0);
    const sf::Color Theme::Scrim = sf::Color(0, 0, 0);
    const sf::Color Theme::InverseSurface = sf::Color(226, 226, 233);
    const sf::Color Theme::InverseOnSurface = sf::Color(46, 48, 54);
    const sf::Color Theme::InversePrimary = sf::Color(65, 95, 145);
    const sf::Color Theme::PrimaryFixed = sf::Color(214, 227, 255);
    const sf::Color Theme::OnPrimaryFixed = sf::Color(0, 27, 62);
    const sf::Color Theme::PrimaryFixedDim = sf::Color(170, 199, 255);
    const sf::Color Theme::OnPrimaryFixedVariant = sf::Color(40, 71, 119);
    const sf::Color Theme::SecondaryFixed = sf::Color(218, 226, 249);
    const sf::Color Theme::OnSecondaryFixed = sf::Color(19, 28, 43);
    const sf::Color Theme::SecondaryFixedDim = sf::Color(190, 198, 220);
    const sf::Color Theme::OnSecondaryFixedVariant = sf::Color(62, 71, 89);
    const sf::Color Theme::TertiaryFixed = sf::Color(250, 216, 253);
    const sf::Color Theme::OnTertiaryFixed = sf::Color(40, 19, 46);
    const sf::Color Theme::TertiaryFixedDim = sf::Color(221, 188, 224);
    const sf::Color Theme::OnTertiaryFixedVariant = sf::Color(87, 62, 92);
    const sf::Color Theme::SurfaceDim = sf::Color(17, 19, 24);
    const sf::Color Theme::SurfaceBright = sf::Color(55, 57, 62);
    const sf::Color Theme::SurfaceContainerLowest = sf::Color(12, 14, 19);
    const sf::Color Theme::SurfaceContainerLow = sf::Color(25, 28, 32);
    const sf::Color Theme::SurfaceContainer = sf::Color(29, 32, 36);
    const sf::Color Theme::SurfaceContainerHigh = sf::Color(40, 42, 47);
    const sf::Color Theme::SurfaceContainerHighest = sf::Color(51, 53, 58);
    const sf::Color Theme::Disabled = sf::Color(90, 90, 90);
}